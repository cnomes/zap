import styled from "styled-components"

const List = styled.ul`
  display: inline-flex;
  flex-wrap: wrap;
  justify-content: space-around;

  width: auto;
  margin: 0;
  padding: 10px;
`

export default List
